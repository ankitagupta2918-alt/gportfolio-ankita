# Ankita Gupta Portfolio

GitHub Pages-ready static portfolio.

## Upload
Upload **all files in this folder** to the root of your GitHub repository. `index.html` must be in the repository root.

Certificate images are embedded directly in `index.html`, so no separate image folder is required.
